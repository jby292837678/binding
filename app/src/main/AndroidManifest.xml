<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.binding.demo">

    <application
        android:name=".ui.IkeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >


        <activity android:name=".ui.home.HomeActivity" >

        </activity>


        <activity android:name=".ui.start.StartupActivity" > </activity>
        <activity android:name=".ui.password.PasswordActivity" >
        </activity>
        <activity android:name=".ui.message.MessageActivity" >

        </activity>
        <activity android:name=".ui.bottom.BottomSheetActivity" >

        </activity>
        <activity android:name=".ui.select.SelectActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

    </application>
</manifest>
